/// Eventsliner Live — event-specific attendee app (Dreamcast Stage 3).
///
/// Built with Flutter for iOS + Android store builds. One binary can be
/// white-labeled per event via `--dart-define` (slug, API, brand color, name).
library;

export 'app.dart';
export 'config/app_config.dart';
