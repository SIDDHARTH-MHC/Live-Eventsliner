# Eventsliner.live Deployment - Manual Steps Required

## ⚠️ BLOCKED: 2FA Authentication Issue

The automated deployment process was blocked due to **2-Factor Authentication** on the Gmail account (eventsliner.live@gmail.com). The account requires a recovery email verification that cannot be bypassed from the cloud agent.

---

## What Was Attempted

### ✅ Successfully Verified
1. **Workspace**: Full Eventsliner app code exists in /workspace
2. **Git Setup**: Repository configured with remote: git@github.com:SIDDHARTH-MHC/Live-Eventsliner.git
3. **Render Config**: render.yaml exists and is properly configured
4. **GitHub Repo**: Confirmed repository exists at https://github.com/SIDDHARTH-MHC/Live-Eventsliner (currently empty)

### ❌ Blocked Tasks
1. **Gmail Access**: 2FA requires recovery email (sid***********@gmail.com) - cannot access
2. **Render Email Verification**: Requires clicking verification link in Gmail
3. **GitHub Login**: Requires Gmail verification or direct GitHub credentials
4. **Code Push to GitHub**: SSH key not available in cloud VM; HTTPS requires token

---

## 📋 MANUAL STEPS YOU MUST COMPLETE

### Step 1: Access Gmail
1. Open https://mail.google.com in a browser
2. Sign in to **eventsliner.live@gmail.com** with password: `Siddharth1@`
3. Complete the 2FA challenge:
   - You'll need access to the recovery email: `sid***********@gmail.com`
   - OR use a phone number associated with the account
   - OR use a previously trusted device

### Step 2: Verify Render Email
1. Once in Gmail, find the email from Render (check Inbox/Spam)
2. Click the verification link in the email
3. This will activate your Render account

### Step 3: Push Code to GitHub

**Option A: Via GitHub Web UI (Easiest)**
1. Log into GitHub as SIDDHARTH-MHC
2. Go to https://github.com/SIDDHARTH-MHC/Live-Eventsliner
3. Click "uploading an existing file"
4. Download the code tarball from cloud agent: `/tmp/eventsliner-code.tar.gz` (374KB)
5. Extract locally and upload all files via GitHub web interface

**Option B: Via Git CLI (If you have SSH/Token)**
1. From your local machine with the Eventsliner code:
   ```bash
   cd /path/to/eventsliner
   git remote add origin git@github.com:SIDDHARTH-MHC/Live-Eventsliner.git
   git push -u origin main
   ```

**Option C: Use GitHub Personal Access Token**
1. Log into GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token with `repo` scope
3. From workspace:
   ```bash
   cd /workspace
   git remote set-url origin https://SIDDHARTH-MHC:<TOKEN>@github.com/SIDDHARTH-MHC/Live-Eventsliner.git
   git push -u origin main
   ```

### Step 4: Deploy to Render
1. Log into https://dashboard.render.com
2. Click **New** → **Blueprint**
3. Connect your GitHub account (if not already connected)
4. Select the `SIDDHARTH-MHC/Live-Eventsliner` repository
5. Render will detect `render.yaml` and show the services:
   - Web Service: `eventsliner` (Node.js, Free plan)
   - PostgreSQL: `eventsliner-db` (Free plan)
   - Redis: `eventsliner-redis` (Free plan)
6. Click **Apply**
7. Wait for deployment to complete (5-10 minutes)

### Step 5: Configure Environment Variables
After deployment, you need to set these environment variables in Render dashboard:

**Required (Set these in Render dashboard):**
- `APP_URL`: Will be provided by Render (e.g., `https://eventsliner-abc123.onrender.com`)
- `RESEND_API_KEY`: Your Resend API key for email
- `MSG91_AUTH_KEY`: Your MSG91 auth key for SMS
- `MSG91_TEMPLATE_ID`: Your MSG91 template ID
- `S3_ENDPOINT`: Your S3-compatible storage endpoint
- `S3_BUCKET`: Your S3 bucket name
- `S3_ACCESS_KEY_ID`: Your S3 access key
- `S3_SECRET_ACCESS_KEY`: Your S3 secret key
- `S3_PUBLIC_URL`: Public URL for S3 assets
- `RAZORPAY_KEY_ID`: Your Razorpay key ID
- `RAZORPAY_KEY_SECRET`: Your Razorpay secret
- `RAZORPAY_WEBHOOK_SECRET`: Your Razorpay webhook secret
- `SENTRY_DSN`: (Optional) Your Sentry DSN

**Auto-generated by Render:**
- `SESSION_SECRET`: ✓ (auto-generated)
- `CRON_SECRET`: ✓ (auto-generated)
- `DATABASE_URL`: ✓ (from eventsliner-db)
- `REDIS_URL`: ✓ (from eventsliner-redis)

### Step 6: Update APP_URL
Once deployment completes:
1. Render will provide a URL like: `https://eventsliner-xyz123.onrender.com`
2. Go to Render dashboard → eventsliner service → Environment
3. Update `APP_URL` to your actual Render URL
4. Trigger a manual redeploy

### Step 7: Verify Deployment
1. Visit your Render URL: `https://eventsliner-xyz123.onrender.com`
2. Check health endpoint: `https://eventsliner-xyz123.onrender.com/health`
   - Should return: `{"status":"ok"}`
3. Test the application:
   - Visit home page
   - Try creating an account
   - Check organizer registration flow

---

## 🔒 SECURITY REMINDER

**CRITICAL:** The password `Siddharth1@` was shared in this chat for deployment purposes.

**IMMEDIATELY AFTER DEPLOYMENT:**
1. Change Gmail password: https://myaccount.google.com/security
2. Change Render password
3. Change GitHub password (if using same credentials)
4. Enable 2FA with backup codes/authenticator app
5. Review and revoke any unnecessary access

---

## 📦 Code Archive

A tarball of the complete codebase (excluding node_modules, .git, .env) has been created:
- **Location**: `/tmp/eventsliner-code.tar.gz`
- **Size**: 374 KB
- **Contents**: All source code, configs, render.yaml, package.json, etc.

You can download this file from the cloud agent and upload it to GitHub manually if needed.

---

## 🚀 Expected Render Services After Deployment

Based on your `render.yaml`:

1. **Web Service: eventsliner**
   - Runtime: Node.js
   - Plan: Free
   - Region: Singapore
   - Build: `pnpm install && pnpm db:migrate:deploy && pnpm build`
   - Start: `pnpm start`
   - Port: 10000

2. **PostgreSQL: eventsliner-db**
   - Database: eventsliner
   - Plan: Free
   - Region: Singapore

3. **Redis: eventsliner-redis**
   - Plan: Free
   - Region: Singapore
   - Policy: allkeys-lru

---

## 📞 Support

If you encounter issues:
1. Check Render logs: Dashboard → Service → Logs
2. Verify all environment variables are set
3. Ensure GitHub repository has all code
4. Check database migrations ran successfully
5. Verify health endpoint returns OK

---

**Summary**: Gmail 2FA blocked all automated steps. You must manually complete Gmail verification, push code to GitHub, and deploy to Render following the steps above.

