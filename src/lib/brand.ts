/** Canonical product display name (not domain). */
export const PRODUCT_NAME = "Eventsliner Live";

/** Public site / email domain — keep as address, not display brand. */
export const PRODUCT_DOMAIN = "eventsliner.live";
